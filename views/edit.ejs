<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
  </head>
  <body>
    <div class="w-full min-h-full bg-zinc-900 p-10 text-white">
      <nav class="mb-6">
        <a class="text-blue-500" href="/read">Cancel</a>
      </nav>
      <h1 class="text-3xl tracking-tighter mb-6">Update User</h1>
      <form
        class="flex gap-3 flex-wrap"
        action="/update/<%=user.id%>"
        method="post"
      >
        <input
          class="px-5 py-2 border-2 border-zinc-800 bg-transparent rounded-md outline-none"
          type="text"
          placeholder="name"
          name="name"
          value="<%=user.name%>"
          maxlength="40"
        />
        <input
          class="px-5 py-2 border-2 border-zinc-800 bg-transparent rounded-md outline-none"
          type="email"
          placeholder="email"
          name="email"
          value="<%= user.email %>"
          maxlength="40"
        />
        <input
          class="px-5 py-2 border-2 border-zinc-800 bg-transparent rounded-md outline-none"
          type="text"
          placeholder="image URL"
          name="image"
          value="<%= user.image %>"
        />
        <input
          class="px-5 py-2 bg-yellow-500 rounded-md hover-pointer"
          type="submit"
          value="Update user"
        />
      </form>
    </div>
  </body>
</html>
